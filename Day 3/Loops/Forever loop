module forever_loop;
  
  logic clk;
  
  initial begin
    
    clk = 0;
    forever 
      #5 clk = ~clk;
     end
  
   initial begin
     
     $monitor("Time = %0t,clk = %0d",$time,clk);
     #50 $finish; 
   end
endmodule
